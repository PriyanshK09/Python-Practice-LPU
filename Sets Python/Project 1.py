# A SET in Python is a collection which is unordered and unindexed. No duplicate members.
# Denoted by curly brackets "{}"

mySet = {"apple", "banana", "cherry"}
print(mySet) #Output willnot be in the same order as entered because sets are unordered and unindexed.

